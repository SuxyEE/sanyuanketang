# .dockerignore — 让 deploy/Dockerfile 的构建上下文尽量小
# 越小 = 上传 daemon 越快、缓存越稳、镜像越瘦

# ── 大的、构建用不到 ──
**/node_modules
**/dist
**/build
**/.turbo
**/.next
**/out

# ── Git / 编辑器 / CI ──
.git
.gitignore
.gitattributes
.github
.vscode
.idea
.cursor
*.swp
*.swo

# ── 测试 / 截图 / 演示资产（仅根级的大文件，不递归覆盖业务图片） ──
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
/*.mp4
screenshots
.playwright-mcp

# ── 文档（只排根级和子模块顶层的 docs / README，不递归覆盖 src 里的 .md） ──
/README.md
/docs
**/docs
apps/*/README.md
packages/*/README.md
uniapp-*/README.md
uniapp-*/docs

# ── 环境变量 / 凭据（强警告：不能进镜像） ──
**/.env
**/.env.local
**/.env.*.local

# ── 临时 / 日志 ──
**/*.log
**/logs
**/tmp
**/.DS_Store
**/Thumbs.db

# ── uniapp 工程的编译产物（apk 走 HBuilderX 云打包，不在容器里出包） ──
# 但 package.json 必须保留（Dockerfile 用 COPY 单独捞），所以这里只排掉真没用的
uniapp-student/unpackage
uniapp-teacher/unpackage
uniapp-student/src
uniapp-teacher/src
